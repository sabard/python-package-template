# _subdirectory: "{{package_name}}"
_templates_suffix: .j2

_envops:
  keep_trailing_newline: true

# _tasks:
#   - "curl https://raw.githubusercontent.com/github/gitignore/main/Python.gitignore > .gitignore"

package_name:
  type: str
  default: python_project

author_email:
  type: str

author_first_name:
  type: str

author_last_name:
  type: str

author_full_name:
  type: str
  default: "{{author_first_name}} {{author_last_name}}"

python_version:
  type: str
  default: 3.10.14

python_requires:
  type: str
  default: ">=3.8"

pytest: True
